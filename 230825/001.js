function 나의스크릿라면레시피(){
    let 라면그릇;

    물 550ml를 끓인다;
    면과 분말 스프, 후레이크를 같이 넣는다;
    4분 40초간 더 끓인다;

    라면그릇 = 맛있는 라면;

    return 라면그릇;

}

나의시크릿라면레시피()

// 함수 구조

function 함수이름(parameter1, parameter2...) {
    // 함수의 선언, 실행코드
    return
}

함수이름(argument1, argument2...) // 함수의 호출

parameter1(매개변수)-함수와 매서드에 입력 변수 이름
argument(전달인자,인자,인수)-함수와 메서드에 실제 입력되는 값

